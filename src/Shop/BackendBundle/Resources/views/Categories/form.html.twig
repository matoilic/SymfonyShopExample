<form action="{{action}}" method="post" name="category">
    {{form_errors(form)}}

    {{form_row(form.name)}}

    <p>
        <input type="submit" value="{% trans %}save{% endtrans %}">
    </p>

    {{form_rest(form)}}
</form>