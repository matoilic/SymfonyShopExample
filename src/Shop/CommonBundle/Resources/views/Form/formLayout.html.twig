{% block field_row %}
    {% spaceless %}
        {% if errors|length > 0%}<p class="error">{% else %}<p>{% endif %}
            {{ form_label(form, label|default(null)) }}<br>
            {{ form_widget(form) }}
        </p>
        {{ form_errors(form) }}
    {% endspaceless %}
{% endblock field_row %}